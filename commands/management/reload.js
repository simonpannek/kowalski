module.exports = {
    name: "reload",
    description: "Reloads the command modules.",
    owner: true,
    async execute(message) {
        // TODO
        return message.channel.send("Pong!");
    }
};
